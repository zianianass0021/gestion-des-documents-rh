{% extends 'base.html.twig' %}

{% block title %}Ajouter un Placard{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="fas fa-plus me-2"></i>Ajouter un Placard
                    </h5>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                        <div class="mb-3">
                            {{ form_label(form.name) }}
                            {{ form_widget(form.name) }}
                            {{ form_errors(form.name) }}
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.location) }}
                            {{ form_widget(form.location) }}
                            {{ form_errors(form.location) }}
                        </div>

                        <div class="d-flex justify-content-between">
                            <a href="{{ path('responsable_manage_placards') }}" class="btn btn-secondary">
                                <i class="fas fa-arrow-left me-2"></i>Retour
                            </a>
                            <button type="submit" class="btn btn-success">
                                <i class="fas fa-save me-2"></i>Enregistrer
                            </button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>

            <div class="card mt-4">
                <div class="card-header">
                    <h6 class="mb-0">
                        <i class="fas fa-info-circle me-2"></i>Informations
                    </h6>
                </div>
                <div class="card-body">
                    <ul class="mb-0">
                        <li>Le nom du placard doit être unique et descriptif</li>
                        <li>La localisation doit indiquer l'emplacement physique du placard</li>
                        <li>Les dossiers seront organisés dans ce placard</li>
                        <li>Vous pourrez modifier ces informations plus tard</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
