{% extends 'base.html.twig' %}

{% block title %}Nouvelle Demande/Réclamation{% endblock %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    <h5 class="mb-0">
                        <i class="fas fa-plus me-2"></i>Nouvelle Demande/Réclamation
                    </h5>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                        <div class="mb-3">
                            {{ form_label(form.titre) }}
                            {{ form_widget(form.titre) }}
                            {{ form_errors(form.titre) }}
                        </div>

                        <div class="mb-3">
                            {{ form_label(form.contenu) }}
                            {{ form_widget(form.contenu) }}
                            {{ form_errors(form.contenu) }}
                        </div>

                        <div class="d-flex justify-content-between">
                            <a href="{{ path('employee_demandes') }}" class="btn btn-secondary">
                                <i class="fas fa-arrow-left me-2"></i>Retour
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-paper-plane me-2"></i>Envoyer la demande/réclamation
                            </button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>

            <div class="card mt-4">
                <div class="card-header">
                    <h6 class="mb-0">
                        <i class="fas fa-lightbulb me-2"></i>Conseils
                    </h6>
                </div>
                <div class="card-body">
                    <ul class="mb-0">
                        <li>Soyez précis dans le titre de votre demande</li>
                        <li>Décrivez clairement votre demande dans le contenu</li>
                        <li>Exemples : demandes de congés, réclamations, demandes de formation, etc.</li>
                        <li>Votre responsable RH vous répondra dans les plus brefs délais</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
